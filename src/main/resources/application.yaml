spring:
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://127.0.0.1:3000/shoppingcart
    username: root
    password: tanvi
    driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8081